# SEC API and Data Collection
sec-api==1.0.15

# Logging
loguru==0.7.2

# HTTP requests - Updated to satisfy docling
requests>=2.32.3

# HTML to Markdown conversion
docling>=2.0.0  # Using 2.x is recommended
markdownify==0.11.6
# Training dependencies
unsloth  # Fast QLoRA fine-tuning
transformers>=4.45.0
datasets>=2.16.0
peft>=0.7.0
trl>=0.9.0
pydantic>=2.0  # JSON validation
accelerate>=0.25.0
bitsandbytes>=0.41.0
wandb  # Optional: training metrics logging
